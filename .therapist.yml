actions:
  flake8:
    run: flake8 {files}
    include: "*.py"
    exclude: "docs/"
  eslint:
    run: eslint {files}
    include:
      - "*.js"
      - "*.jsx"
  stylelint:
    run: stylelint {files}
    include:
      - "*.css"
      - "*.scss"
