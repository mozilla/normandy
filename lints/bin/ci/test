#!/usr/bin/env bash
set -eu

echo "Running lint tests"
REPO_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && cd ../../.. && pwd)"

echo docker run \
  --net host \
  --volume $REPO_DIR:/app/code \
  normandy-lint:build
